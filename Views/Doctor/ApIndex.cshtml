@model List<Cms.Web.ViewModel.DoctorViewModel>
@{
    ViewData["Title"] = "ApIndex";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

<h1>ApIndex</h1>

<table class="table">
    <thead>
        <tr>
            <th scope="col">#</th>
            <th scope="col">İsmi</th>
            <th scope="col">Ünvan</th>
            <th scope="col">Policlinic</th>
            <th scope="col"></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <th scope="row">@item.Id</th>
                <td>@item.FullName</td>
                <td>@item.title</td>
                <td>@item.PoliclinicName</td>
                <td><a asp-action="Delete" asp-controller="doctor" asp-route-id="@item.Id" class="btn btn-danger">Sil</a></td>
            </tr>
        }
    </tbody>
</table>
<a asp-controller="Doctor" asp-action="Add" class="btn btn-primary btn-lg btn-block">Doktor Ekle</a>